name: Calling workflow test-vinod_pal

on:
 workflow_dispatch:
   inputs:
      environment:
        type: environment
 schedule:
   - cron : '55 13 * * *'
 

jobs:
  call-workflow:
    uses: ./.github/workflows/deploy.yml
    secrets: inherit
    permissions:
     id-token: write
     contents: write

